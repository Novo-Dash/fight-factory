# Tracking e webhooks de booking NÃO usam env — são constantes no código:
#  - Webhook 1 (lead → GHL/LeadConnector) e Webhook 2 + slots (n8n) ficam em
#    src/booking/webhook.ts. Webhook 2 = URL fixa de produção
#    (https://n8n.novodash.com/webhook/landing-page-booking).
#  - Meta Pixel: base no index.html (id 4326414901006955); eventos via fbq no
#    código (src/booking/analytics.ts).
#  - GA4 + Google Ads: tag base gtag.js no index.html; eventos/conversões via
#    gtag no código. Google Ads = AW-18177687947. GA4 ainda não fornecido —
#    quando vier (G-XXXXXXX), habilitar o config no index.html.
#  - SEM GTM no stack (removido).
